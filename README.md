# CROP-YIELD-PREDICTION
This project uses a `RandomForestRegressor` to predict Indian crop production based on state, crop, season, and area, deploying the final model into an interactive Streamlit web app.
